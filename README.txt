نسخه اصلاح کاربران آفلاین V2

در این نسخه خطای JavaScript نسخه قبل برطرف شده است.

تغییرات:
- تعریف کاربر آفلاین فعال است.
- لیست کاربران از localStorage نمایش داده می‌شود.
- ورود با کاربران ساخته‌شده کار می‌کند.
- کاربر admin ثابت می‌ماند.
- رمز admin از بخش رمز ورود آفلاین تغییر می‌کند.

فقط این دو فایل را جایگزین کن:
index.html
tak-ui.css

برای تست:
?usersfix=2
