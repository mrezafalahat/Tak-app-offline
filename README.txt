نسخه کامل نهایی با اصلاح ورود

تغییرات:
- فیلد نام کاربری در صفحه ورود دیگر admin پیش‌فرض ندارد.
- فیلد نام کاربری و رمز عبور placeholder دارند.
- admin قابل حذف نیست.
- رمز admin از تنظیمات مدیر > رمز ادمین قابل تغییر است.
- کاربران آفلاین ذخیره و قابل ورود هستند.
- آیکون‌های منو داخل خود HTML هستند و پوشه icons لازم نیست.

آپلود:
فقط این دو فایل را جایگزین کن:
index.html
tak-ui.css

تست:
?loginblank=1
