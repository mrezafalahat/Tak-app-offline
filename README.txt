نسخه نهایی مدیریت ادمین

تغییرات:
- کاربر admin قابل حذف نیست.
- admin از فرم کاربران قابل ویرایش نیست.
- رمز admin از تنظیمات مدیر > رمز ادمین قابل تغییر است.
- رمز جدید در localStorage ذخیره می‌شود.
- اگر رمز admin تغییر کند، ورود بعدی با رمز جدید انجام می‌شود.

آپلود:
index.html
tak-ui.css

تست:
?adminpass=1
