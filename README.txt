نسخه اصلاح رمز آفلاین

تغییرات:
- رمز آفلاین دیگر ثابت نیست.
- از تنظیمات مدیر می‌توان رمز ورود آفلاین را تغییر داد.
- پیام خطای ورود دیگر رمز پیش‌فرض را نمایش نمی‌دهد.
- رمز اولیه همچنان 1234 است، مگر اینکه تغییرش بدهی.

آپلود:
فقط این دو فایل را جایگزین کن:
index.html
tak-ui.css

برای تست:
?passfix=1
